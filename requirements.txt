# ─────────────────────────────────────────────────────────────────────────────
# PARAM-BRAHMAND (विश्वरूप-AI) — Python Dependencies
# Smart India Hackathon 2026 · ISRO SAC PS 26167
# ─────────────────────────────────────────────────────────────────────────────

# ── API & Serving ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0
starlette>=0.37.0

# ── Numerical / Scientific ─────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0

# ── Deep Learning (optional — engines run in pure Python without torch) ───────
torch>=2.2.0          # Geo-Mamba 3.0 training backbone (skip if CPU-only demo)
torchvision>=0.17.0   # Image transforms for BigEarthNet / VRSBench

# ── Geospatial ────────────────────────────────────────────────────────────────
requests>=2.31.0      # COG HTTP streaming (cog_streamer.py)

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0     # Coverage reports for CI

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0  # .env configuration loading
